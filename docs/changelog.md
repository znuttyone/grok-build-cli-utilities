# Changelog

All notable changes are documented in the repository root [CHANGELOG.md](https://github.com/cobusgreyling/grok-build-cli-utilities/blob/main/CHANGELOG.md).

The latest release notes are also shown on the [GitHub Releases](https://github.com/cobusgreyling/grok-build-cli-utilities/releases) page.

## Recent highlights (0.4.0)

- Token-accurate `usage cost` and `usage report` with **list$ + est$** (cash_scale / toml)
- **`--plan-advisor` / `-P`**: API vs SuperGrok vs Heavy break-even for a window
- Novice FAQ (`usage info`): Session Cost vs list$/est$, Credits, weekly limit, auto top-up
- Date-span titles, open-ended `--from`, clearer option help

## 0.3.x

- Expanded Tier 1/2 commands: plugins, hooks, config, logs, sessions export + analyze
- Shared TOML loader, safer FS/JSON helpers, macOS CI, doctor polish

See the full `CHANGELOG.md` in the repo for the complete history.
